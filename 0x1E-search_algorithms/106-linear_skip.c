#include <stdio.h>
#include <stdlib.h>
#include "search_algos.h"
/**
 * linear_skip - This function search a value in an array of integer
 * using the lineary search algorithms
 * @list: An pointer to the first element of the array
 * @value: The value to search
 *
 * Return: node or NULL
 */
skiplist_t *linear_skip(skiplist_t *list, int value)
{
	if (!list)
		return (NULL);
	return (NULL);
}
